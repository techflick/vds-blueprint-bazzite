# yaml-language-server: $schema=https://blue-build.org
name: mein-bazzite-vds
description: Mein persönliches Bazzite mit automatischer Aktivierung des vDS Treibers.

base-image: ghcr.io/ublue-os/bazzite
image-version: latest

modules:
  - type: script
    snippets:
      - |
        echo "=== 1. Benötigte Build-Tools im Image installieren ==="
        # Werkzeuge für die Cloud-Kompilierung bereitstellen
        rpm-ostree install kernel-devel make gcc git

        echo "=== 2. vDS Quellcode klonen und in den Treiber-Ordner wechseln ==="
        git clone https://github.com/hurryman2212/vds.git /tmp/vds_source
        
        # HIER REPARIEREN WIR DEN PFAD: Wir wechseln gezielt in den Kernel-Modul-Ordner
        cd /tmp/vds_source/vds_hcd
        
        # Jetzt existiert das Makefile und der Build funktioniert!
        make
        make install

        echo "=== 3. Kernel-Modul für den Systemstart registrieren ==="
        mkdir -p /etc/modules-load.d
        echo "vds_hcd" > /etc/modules-load.d/vds.conf

        echo "=== 4. Bluetooth-Anpassung aktivieren ==="
        mkdir -p /etc/systemd/system/bluetooth.service.d
        cat << 'EOF' > /etc/systemd/system/bluetooth.service.d/override.conf
        [Service]
        ExecStart=
        ExecStart=/usr/libexec/bluetooth/bluetoothd --noplugin=input
        EOF
